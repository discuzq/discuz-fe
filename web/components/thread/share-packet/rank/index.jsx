import React, { useState } from 'react';
import styles from './index.module.scss';
import { Button, Tabs } from '@discuzq/design';


const content = ({ visible = false, type = '' }) => {
  return (
    <div className={styles.root}>
      123
    </div>
  );
};

export default content;
